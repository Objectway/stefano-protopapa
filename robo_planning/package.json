{
  "name": "package.json",
  "version": "1.0.0",
  "description": "",
  "main": "index.html",
  "scripts": {
    "watch": "pug index.pug --watch",
    "watch1": "sass --watch style:style",
    "dev": "concurrently \"npm run watch\" \"npm run watch1\"", 
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^4.1.0",
    "pug": "^2.0.3"
  }
}
