mixin dataList(name, surname)
    tr
        td= name
        td= surname

doctype html
html
    head
        title My Pug
    body
        div
            h1 My PUG
            div
                table(border="2", cellspacing="2", cellpadding="2")
                    +dataList("mario", "Bianchi")
                    +dataList("mario", "Bianchi")
                    +dataList("mario", "Bianchi")
                    +dataList("mario", "Bianchi")